#include <stdio.h>
int main(){
       char alph;
       scanf("%c",&alph);
       if(alph>=A && alph<=Z){
        printf("Uppercase",alph);
       }
       else{
        printf("Lowercase",alph)
       }

}